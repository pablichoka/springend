name: CI Workflow

on: 
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-typos:
    name: typos-check
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: typos-action
      uses: crate-ci/typos@v1.22.7
